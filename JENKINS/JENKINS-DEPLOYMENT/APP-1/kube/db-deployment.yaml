apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: app-1

spec:
  replicas: 1

  selector:
    matchLabels:
      app: app1-db

  template:
    metadata:
      labels:
        app: app1-db

    spec:
      containers:
        - name: mysql
          image: docker.io/payalkharat/kubernetes-ingress:app-1-db-v2

          ports:
            - containerPort: 3306

          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app1-db-secret
                  key: MYSQL_ROOT_PASSWORD

            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: app1-db-config
                  key: MYSQL_DATABASE

          args:
            - --default-authentication-plugin=mysql_native_password

          volumeMounts:
            - name: db-storage
              mountPath: /var/lib/mysql

      volumes:
        - name: db-storage
          persistentVolumeClaim:
            claimName: app1-db-pvc
